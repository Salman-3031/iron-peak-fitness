{
  "name": "iron-peak",
  "version": "1.0.0",
  "main": "index.js",
  "homepage": "./",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npx tailwindcss -i src/input.css -o dist/output.css && cp index.html dist/ && cp -r pages/ src/assets/ dist/",
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17"
  }
}
