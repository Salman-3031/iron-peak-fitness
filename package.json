{
  "name": "iron-peak",
  "version": "1.0.0",
  "main": "index.js",
  "homepage": "./",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir -p dist && npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp index.html ./dist/ && cp -r pages/ ./dist/pages/ && cp -r src/assets/ ./dist/assets/",
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17"
  }
}
